export function isPositiveNumber(number: number): boolean {
    return +number > 0;
}
export function stringValidation(str: string, maxLen: number): boolean {
    return str.length > maxLen;
}
export function isValidc(number: number) : boolean {
    return true
}

